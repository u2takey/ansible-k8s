apiVersion: v1
kind: Service
metadata:
  name: {{ meta.name }}
  namespace: {{ meta.namespace }}
spec:
  selector:
    app: {{ meta.name }}
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80